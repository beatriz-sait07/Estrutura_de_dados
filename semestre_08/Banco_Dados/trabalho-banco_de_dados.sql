--1
SELECT codigo, tipo_sanguineo
FROM fakes
LIMIT 10;

--2
SELECT codigo, cpf, tipo_sanguineo
FROM fakes
LIMIT 10;

--3
SELECT codigo, nome, nome_meio, sobrenome, endereco, cidade
FROM fakes
LIMIT 10;

--4
SELECT codigo, cidade, email, usuario, senha
FROM fakes
LIMIT 15;

--5
SELECT codigo, cidade, email, usuario, senha
FROM fakes
LIMIT 25;

--6
SELECT codigo, cidade, email, usuario, senha
FROM fakes
LIMIT 13;

--7
SELECT cidade, cep, endereco, latitude, longitude
FROM fakes
LIMIT 25;

--8
SELECT city, state
FROM fakes
LIMIT 12;

--9
SELECT city, state
FROM fakes
LIMIT 8;

--10
SELECT city, state
FROM fakes
LIMIT 8;









