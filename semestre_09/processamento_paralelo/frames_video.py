"""

***o que foi feito
for frames
    for linha
        for coluna
            inserir cores

**o que fazer
usar numpy -> para retirar os for linha,coluna"""

import numpy as np
import random

tempo = 3600
frame_por_segundo = 30
resolucao_altura = 3840
resolucao_largura = 2160